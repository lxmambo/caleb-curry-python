healthy = ['pizza',"Frozen Custard"]

#lists are dinamic. it can grow or shrink.

healthy.append('apple crisps')

#healthy is an object of type list. methods can be applied to objects
#a method is a function attached to an objects
print(healthy)

length = len(healthy) #<-stand alone function, not attached to an object

print("length:",length)